<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicolas.ccontrol" >

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity1"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DiagramActivity1"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
        <activity
            android:name=".AddCatActivity1"
            android:theme="@style/Base.Theme.AppCompat.Dialog" />
        <activity android:name=".ItemActivity1" />
        <activity android:name=".SourceActivity" />
        <activity
            android:name=".AddItemActivity1"
            android:theme="@style/Base.Theme.AppCompat.Dialog" />
        <activity android:name=".ChangeItemActivity1" />
        <activity android:name=".GetImageActivity1" />
        <activity android:name=".ImageActivity1" >
        </activity>
    </application>

</manifest>
